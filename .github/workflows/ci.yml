name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: List Files
        run: ls

      - name: Build Step
        run: echo "Build Successful"

      - name: Slack Notification
        if: success()
        run: |
          curl -X POST -H 'Content-type: application/json' \
          --data '{"text":"🚀 GitHub Actions CI Successful for devops-webapp!"}' \
          ${{ secrets.SLACK_WEBHOOK }}
